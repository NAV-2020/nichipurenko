
"""
class Dog:
    total_count = 0

    def __init__(self, name: str, age: int):
        Dog.total_count += 1
        self.name = name
        self.age = age

    def say_wof(seif):
        print('-WOF-')

    def say_name(self):
        print(f"--WOF, my name is{self.name}")

if __name__ == '__main__':
    barbos = Dog(name = "Barbos", age = 7)
    tuzik = Dog(name = "Tuzik", age = 4)

    
    barbos.say_wof()
    barbos.say_name()

    tuzik.say_wof()
    tuzik.say_name()

    print(barbos.name)
    print(barbos.age)
    
    print(tuzik.name)
    print(tuzik.age)

    Dog.say_name(Dog(name='Dooog', age=10))


    dogs = Dog.total_count
    print(dogs)

    
    print(tuzik)
    print(barbos)
    print(Dog)
    

    class Car():
        def __init__(self, brand: str, model: str, year: int):
            self. brend = brand
            self.model = model
            self.year = year
        
        def set_engine_volume(self):
            self.volume = int(imput("Enter the volume of engine: "))
            return self.volume

        def  set_color(solf):
            colors = ['red', 'blue', 'green']
            self.color = random.choico(colors)
            return self.colors

    if __name__ == '__main__'
        my_car = Car('audi', 'a6', 2019)

        print(my_car.brend)
        print(my_car.model)
        print(my_car.year)

        my_car.set_engine_volume()
        print(my_car.volume)

        my_car.set_color()
        print(my_car.color)
        """